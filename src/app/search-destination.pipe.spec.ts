import { SearchDestinationPipe } from './search-destination.pipe';

describe('SearchDestinationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDestinationPipe();
    expect(pipe).toBeTruthy();
  });
});
